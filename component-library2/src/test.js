import { ExampleComponentA } from '.';
import { Button } from './UI/Button';

describe('ExampleComponentA', () => {
  it('is truthy', () => {
    expect(ExampleComponentA).toBeTruthy()
  })
})

describe('Button', () => {
  it('is truthy', () => {
    expect(Button).toBeTruthy()
  })
})
